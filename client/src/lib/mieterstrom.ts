/**
 * client/src/lib/mieterstrom.ts
 *
 * Re-export of the engine, which lives in shared/ so the server API runs the
 * identical code. Kept as a module so existing client imports are unchanged.
 */
export * from "../../../shared/mieterstrom";
